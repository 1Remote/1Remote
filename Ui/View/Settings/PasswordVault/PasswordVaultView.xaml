<UserControl x:Class="_1RM.View.Settings.PasswordVault.PasswordVaultView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:_1RM.View.Settings.PasswordVault"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance local:PasswordVaultViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <DataGrid AutoGenerateColumns="False"
                  HorizontalAlignment="Stretch" 
                  HorizontalContentAlignment="Stretch" 
                  CanUserSortColumns="False"
                  CanUserResizeColumns="False"
                  CanUserResizeRows="False"
                  CanUserReorderColumns="False"
                  ColumnWidth="*"
                  IsReadOnly="True"
                  ItemsSource="{Binding Credentials}" >
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="{x:Type DataGridColumnHeader}">
                    <Setter Property="VerticalContentAlignment" Value="Center" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridColumnHeader">
                                <Grid Height="35">
                                    <TextBlock Text="{TemplateBinding  Content}" 
                                               FontSize="18" FontWeight="Bold"
                                               Foreground="{DynamicResource BackgroundTextBrush}"
                                               VerticalAlignment="Center" 
                                               HorizontalAlignment="Left" 
                                               TextWrapping="Wrap"/>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </DataGrid.ColumnHeaderStyle>


            <DataGrid.Columns>

                <DataGridTemplateColumn Width="240" Header="{DynamicResource Name}">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding  Credential.Name}" Width="220" TextTrimming="CharacterEllipsis" HorizontalAlignment="Left"></TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Width="240" Header="{DynamicResource User}">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Credential.UserName}" Width="220" TextTrimming="CharacterEllipsis" HorizontalAlignment="Left"></TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>


                <DataGridTemplateColumn Width="100" Header="{DynamicResource Password}">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock HorizontalAlignment="Left">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="******"></Setter>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Path=Credential.Password}" Value="">
                                                <Setter Property="Text" Value=""></Setter>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Width="100" Header="{DynamicResource 'Private key'}">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock HorizontalAlignment="Left">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="******"></Setter>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Path=Credential.PrivateKeyPath}" Value="">
                                                <Setter Property="Text" Value=""></Setter>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>


                <DataGridTemplateColumn Width="150" Header="{DynamicResource 'Data Source name'}">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding DataSource.DataSourceName}" Width="130" TextTrimming="CharacterEllipsis" HorizontalAlignment="Left"></TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Width="Auto" MinWidth="100" Header="">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Button Content="{DynamicResource Edit}"
                                        Margin="5"
                                        Command="{Binding Path=DataContext.CmdEdit, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}" 
                                        CommandParameter="{Binding}">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource ButtonAccentStyle}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Path=DataSource.IsWritable}" Value="False">
                                                    <Setter Property="IsEnabled" Value="False"></Setter>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                                <Button Content="{DynamicResource Delete}" 
                                        Margin="5"
                                        Command="{Binding Path=DataContext.CmdDelete, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}" 
                                        CommandParameter="{Binding}">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource ButtonAccentStyle}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Path=DataSource.IsWritable}" Value="False">
                                                    <Setter Property="IsEnabled" Value="False"></Setter>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>


        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Height="35" HorizontalAlignment="Right">
            <!--ADD BUTTON-->
            <Button Style="{StaticResource ButtonBarAccentStyle}" Command="{Binding CmdAdd}">
                <StackPanel Orientation="Horizontal" Margin="3">
                    <Path Height="12" UseLayoutRounding="True" 
                          Data="{StaticResource GeometryCross}" 
                          Fill="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Button}, Path=Foreground}" 
                          Stretch="Uniform"/>
                    <TextBlock Margin="5" HorizontalAlignment="Left" Text="{DynamicResource Add}" Foreground="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Button}, Path=Foreground}"></TextBlock>
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
