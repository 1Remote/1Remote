<UserControl x:Class="PRM.View.ProtocolConfig.ProtocolConfiger"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PRM.View.ProtocolConfig"
             xmlns:configuration="clr-namespace:PRM.ViewModel.Configuration"
             xmlns:default="clr-namespace:PRM.Core.Protocol.Runner.Default;assembly=PRM.Core"
             xmlns:runner="clr-namespace:PRM.Core.Protocol.Runner;assembly=PRM.Core"
             d:DataContext="{d:DesignInstance configuration:ProtocolConfigerViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Grid >
            <Border BorderBrush="Black" BorderThickness="0 0 1 0" Width="150">
                <ListBox ItemsSource="{Binding Protocols}"
                         SelectedItem="{Binding SelectedProtocol}"
                         Background="Transparent"
                         BorderThickness="0"
                         >
                </ListBox>
            </Border>
        </Grid>

        <Grid Grid.Column="1">
            <Grid.Resources>
                <DataTemplate DataType="{x:Type runner:Runner}">
                    <Grid Background="{DynamicResource PrimaryMidBrush}">
                        <TextBlock Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center" Text="{Binding Name}"></TextBlock>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Grid Margin="5" Height="20">
                                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{DynamicResource PrimaryTextBrush}">(default)</TextBlock>
                            </Grid>
                        </StackPanel>
                    </Grid>
                </DataTemplate>

                <DataTemplate DataType="{x:Type default:SshDefaultRunner}">
                    <local:PuttyRunnerConfiger></local:PuttyRunnerConfiger>
                </DataTemplate>

                <DataTemplate DataType="{x:Type runner:ExternRunner}">
                    <Grid Background="{DynamicResource PrimaryMidBrush}">
                        <TextBlock Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center" Text="{Binding Name}"></TextBlock>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Grid Margin="5" Height="20">
                                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{DynamicResource PrimaryTextBrush}">(default)</TextBlock>
                            </Grid>
                        </StackPanel>
                    </Grid>
                </DataTemplate>

            </Grid.Resources>
            <Border BorderBrush="Black" BorderThickness="0 0 1 0">
                <StackPanel>
                    <TextBlock>TXT:配置</TextBlock>
                    <ListBox ItemsSource="{Binding Runners}" SelectedItem="{Binding SelectedRunner}">
                        <ListBox.Resources>
                            <Style TargetType="ListBox">
                                <Setter Property="Background" Value="Transparent"></Setter>
                                <Setter Property="BorderThickness" Value="0"></Setter>
                            </Style>
                        </ListBox.Resources>
                    </ListBox>

                    <!--<Grid Background="{DynamicResource PrimaryMidBrush}">
                        <TextBlock Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center">KiTTY</TextBlock>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Grid Margin="5" Height="20">
                                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{DynamicResource PrimaryTextBrush}">(default)</TextBlock>
                            </Grid>
                        </StackPanel>
                    </Grid>

                    <local:PuttyRunnerConfiger x:Name="PuttyRunnerConfiger"></local:PuttyRunnerConfiger>

                    <Button Style="{StaticResource ButtonPrimaryStyle}">+</Button>-->
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
