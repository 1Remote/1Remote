<Window x:Class="PersonalRemoteManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls1="clr-namespace:PRM.Controls"
        mc:Ignorable="d"
        BorderThickness="0"
        BorderBrush="Transparent"
        Title="MainWindow" Height="500" Width="680" MinWidth="520" MinHeight="300">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" ResizeBorderThickness="5" UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>

    <Border>
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="BorderBrush" Value="#333"></Setter>
                <Setter Property="BorderThickness" Value="1"></Setter>
                <Setter Property="Margin" Value="0"></Setter>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Window}, Path=WindowState}" Value="Maximized">
                        <Setter Property="Margin">
                            <Setter.Value>
                                <Thickness Left="6" Top="6" Right="6" Bottom="0"></Thickness>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="BorderThickness" Value="0"></Setter>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>


        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"></RowDefinition>
                <RowDefinition Height="20"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>


            <Grid Background="{DynamicResource MainColor1}" Name="WinTitleBar" PreviewMouseDown="System_MouseDown" PreviewMouseMove="System_MouseMove">
                <StackPanel Orientation="Horizontal">
                    <Grid Background="Aquamarine" Width="25" Height="25" Margin="2"></Grid>

                    <Label VerticalContentAlignment="Center" Margin="5,0,105,0" Foreground="{DynamicResource MainColor1Foreground}">
                        <TextBlock Text="{Binding Title, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}}"/>
                    </Label>
                </StackPanel>
            </Grid>

            <!-- System Button Area -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Background="{DynamicResource MainColor1}">
                <StackPanel.Resources>
                    <Style TargetType="{x:Type Button}" x:Key="systemButton">
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Width" Value="35"/>
                        <Setter Property="Height" Value="25"/>
                        <Setter Property="HorizontalAlignment" Value="Right"/>
                        <Setter Property="VerticalAlignment" Value="Top"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type Button}">
                                    <Border Background="{TemplateBinding Background}"  BorderThickness="0">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="Button.IsMouseOver" Value="True">
                                <Setter Property="Button.Background" Value="{DynamicResource MainBgColor}" />
                            </Trigger>
                        </Style.Triggers>

                    </Style>
                </StackPanel.Resources>

                <Grid Height="25" VerticalAlignment="Top" Width="150">
                    <TextBox Width="140" Height="20" VerticalAlignment="Center" HorizontalAlignment="Center" HorizontalContentAlignment="Center"
                             Text="Filter">
                        <TextBox.Style>
                            <Style TargetType="{x:Type TextBox}">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="{x:Type TextBox}">
                                            <Border x:Name="border" Width="Auto" Height="Auto" BorderThickness="1" Cursor="IBeam">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="BorderBrush" Value="Transparent"></Setter>
                                                        <Style.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="BorderBrush" Value="{DynamicResource MainColor2}"></Setter>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <Grid x:Name="grid" Background="#FFFFFF" Opacity="0.5">
                                                    <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                                                </Grid>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                </Grid>
                <Button Name="BtnSetting" Click="BtnSetting_OnClick">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource systemButton}">
                            <Setter Property="Background" Value="{DynamicResource MainColor1}"></Setter>
                            <Style.Triggers>
                                <Trigger Property="Button.IsMouseOver" Value="True">
                                    <Setter Property="Button.Background" Value="{DynamicResource MainBgColor}" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <Canvas Height="25" Width="35" >
                        <Line X1="12" Y1="8" X2="22" Y2="8" Stroke="{DynamicResource MainColor1Foreground}" StrokeThickness="1"/>
                        <Line X1="12" Y1="13" X2="22" Y2="13" Stroke="{DynamicResource MainColor1Foreground}" StrokeThickness="1"/>
                        <Line X1="12" Y1="18" X2="22" Y2="18" Stroke="{DynamicResource MainColor1Foreground}" StrokeThickness="1"/>
                    </Canvas>
                </Button>

                <Button Name="BtnMinimize">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource systemButton}">
                            <Setter Property="Background" Value="{DynamicResource MainColor1}"></Setter>
                            <Style.Triggers>
                                <Trigger Property="Button.IsMouseOver" Value="True">
                                    <Setter Property="Button.Background" Value="{DynamicResource MainBgColor}" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <Rectangle Width="11" Stroke="{DynamicResource MainColor1Foreground}" StrokeThickness="0.75"/>
                </Button>

                <Button Name="BtnMaximize">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource systemButton}">
                            <Setter Property="Background" Value="{DynamicResource MainColor1}"></Setter>
                            <Style.Triggers>
                                <Trigger Property="Button.IsMouseOver" Value="True">
                                    <Setter Property="Button.Background" Value="{DynamicResource MainBgColor}" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <Grid>
                        <Rectangle Width="11" Height="11" Stroke="{DynamicResource MainColor1Foreground}" StrokeThickness="0.75">
                            <Rectangle.Style>
                                <Style TargetType="Rectangle">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Window}, Path=WindowState}" Value="Maximized">
                                            <Setter Property="Visibility" Value="Hidden"></Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Rectangle.Style>
                        </Rectangle>

                        <Canvas>
                            <Canvas.Style>
                                <Style TargetType="Canvas">
                                    <Setter Property="Visibility" Value="Hidden"></Setter>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Window}, Path=WindowState}" Value="Maximized">
                                            <Setter Property="Visibility" Value="Visible"></Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Canvas.Style>
                            <Polyline Points="2.375,2 2.375,0.375 10.625,0.375 10.625,8.625 9,8.625" StrokeThickness="0.75" Stroke="{DynamicResource MainColor1Foreground}"/>
                            <Rectangle Width="9" Height="9" Stroke="{DynamicResource MainColor1Foreground}" StrokeThickness="0.75" Margin="0,2,2,0"/>
                        </Canvas>
                    </Grid>
                </Button>

                <Button Name="BtnClose">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource systemButton}">
                            <Setter Property="Background" Value="{DynamicResource MainColor1}"></Setter>
                            <Style.Triggers>
                                <Trigger Property="Button.IsMouseOver" Value="True">
                                    <Setter Property="Button.Background" Value="Red" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <Canvas Height="25" Width="35" >
                        <Line X1="12" Y1="8" X2="22" Y2="18" Stroke="{DynamicResource MainColor1Foreground}" StrokeThickness="0.75"/>
                        <Line X1="12" Y1="18" X2="22" Y2="8" Stroke="{DynamicResource MainColor1Foreground}" StrokeThickness="0.75"/>
                    </Canvas>
                </Button>
            </StackPanel>



            <Grid Background="#ced8e1" Grid.Row="2">
                <Grid.Resources>
                    <Style TargetType="ListViewItem">
                        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                        <Setter Property="Control.Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListViewItem">
                                    <Border BorderThickness="{TemplateBinding Border.BorderThickness}" 
                                Padding="{TemplateBinding Control.Padding}" 
                                BorderBrush="{TemplateBinding Border.BorderBrush}" 
                                Background="{TemplateBinding Panel.Background}" 
                                Name="Bd" 
                                SnapsToDevicePixels="True">
                                        <ContentPresenter 
                                Content="{TemplateBinding ContentControl.Content}" 
                                ContentTemplate="{TemplateBinding ContentControl.ContentTemplate}" 
                                ContentStringFormat="{TemplateBinding ContentControl.ContentStringFormat}" 
                                HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}" 
                                VerticalAlignment="{TemplateBinding Control.VerticalContentAlignment}" 
                                SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="Selector.IsSelected" Value="True">
                                            <Setter Property="Panel.Background" TargetName="Bd" Value="Transparent"></Setter>
                                        </Trigger>
                                        <Trigger Property="UIElement.IsMouseOver" Value="True">
                                            <Setter Property="Panel.Background" TargetName="Bd" Value="Transparent"></Setter>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Grid.Resources>
                <ListView ItemsSource="{Binding ServerList}" 
                      Padding="10"
                      BorderThickness="0"
                      Background="Transparent"
                      ScrollViewer.HorizontalScrollBarVisibility="Hidden" Grid.ColumnSpan="2" Margin="0,0,0,0.5"
                      >
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" Width="{Binding RelativeSource={RelativeSource FindAncestor,AncestorType=ListView}, Path=ActualWidth}"/>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Width="154" Height="164">
                                <controls1:ServerCard VmServerCard="{Binding}"></controls1:ServerCard>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ListView>
            </Grid>


            <Grid Grid.Row="1" Grid.RowSpan="2" VerticalAlignment="Top">
                <Border BorderThickness="0 0 0 1" BorderBrush="{DynamicResource MainColor2}" Background="{DynamicResource MainColor1Foreground}" Height="20">
                    <Border.Effect>
                        <DropShadowEffect ShadowDepth="3" Direction="270"  Opacity="0.6" Color="Black"></DropShadowEffect>
                    </Border.Effect>
                </Border>

                <Border BorderThickness="0 0 0 2" BorderBrush="{DynamicResource MainColor2}" Background="{DynamicResource MainColor1}">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" Height="20">
                        <Button Padding="0 1 0 0" Width="100" Background="{DynamicResource MainColor2}" Foreground="{DynamicResource MainColor1Foreground}" FontWeight="Bold" BorderThickness="0" Content="{DynamicResource tab_all}"></Button>
                        <Button Padding="0 1 0 0" Background="{DynamicResource MainColor1}" Width="80" BorderThickness="0" Foreground="{DynamicResource MainColor1Foreground}" Content="{DynamicResource confirm}"></Button>
                        <Button Padding="0 1 0 0" Background="{DynamicResource MainColor1}" Width="80" BorderThickness="0" Foreground="{DynamicResource MainColor1Foreground}">123123</Button>
                        <Button Padding="0 1 0 0" Background="{DynamicResource MainColor1}" Width="80" BorderThickness="0" Foreground="{DynamicResource MainColor1Foreground}">123123</Button>
                        <Button Padding="0 1 0 0" Background="{DynamicResource MainColor1}" Width="80" BorderThickness="0" Foreground="{DynamicResource MainColor1Foreground}">123123</Button>
                        <Button Padding="0 1 0 0" Background="{DynamicResource MainColor1}" Width="80" BorderThickness="0" Foreground="{DynamicResource MainColor1Foreground}">123123</Button>
                        <Button Padding="0 1 0 0" Background="{DynamicResource MainColor1}" Width="80" BorderThickness="0" Foreground="{DynamicResource MainColor1Foreground}">123123</Button>
                        <Button Padding="0 1 0 0" Background="{DynamicResource MainColor1}" Width="80" BorderThickness="0" Foreground="{DynamicResource MainColor1Foreground}">123123</Button>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Border>

</Window>
